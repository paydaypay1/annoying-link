// Reference Globals as such:
// $imports.globals.siteTitle
//
// Instantiate after importing like so:
// declare global globals = Globals()

const Globals = () => {
  return {
    siteTitle: "Annoying Link",
    siteDescription: "A website that is simply an annoying URL. "
  }
}
export default Globals;